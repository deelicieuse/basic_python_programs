# initialize even count as zero 
even_count = 0

# prompt 10 numbers thru loop 
for i in range(10):
    number = float(input(f"Enter number {i+1}: "))

# count if divisible by 2
    if number % 2 == 0:
        even_count += 1

# print even count
print(f"The count of even numbers is {even_count}")
# prompt for a dividend and divisor
dividend = float(input("Enter first number: "))
divisor = float(input("Enter second number: "))

# if the divisor is zero
#     then print error
# else print quotient

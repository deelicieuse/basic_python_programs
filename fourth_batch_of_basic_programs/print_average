# intialize empty number list
numbers = []

# prompt for number via loop
while True:
    try:
        num = float(input("Enter Number: "))

# break loop if input is invalid
    except:
        break 
    
# append
# if numbers is not empty
#     compute
#     print
# intialize empty numbers list
numbers = []

# prompt for number via for loop
for i in range(10):
     num = int(input("Enter number: "))

# append
     numbers.append(num)

# initialize empty printed list
printed = []

# check if number is greater than one and not in list via for loop
# print and append

# intialize empty numbers list
numbers = []

# start loop 
while True:
     try:  
# prompt for number 
        num = int(input("Enter number: "))
except:

# check if input is invalid
     break

# check if number is in numbers list
     if num in numbers:
        # print duplicate 
        print("Duplicate")
# else print unique and append
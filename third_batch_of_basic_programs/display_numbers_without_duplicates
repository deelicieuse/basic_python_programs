# intialize empty list for numbers
numbers = []

# prompt for numbers and append
for i in range(10):
    num = int(input("Enter number: "))
    numbers.append(num)
    
# check if numbers have dupes
# print number
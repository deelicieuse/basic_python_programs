# initialize numbers list
numbers = []

# prompt for numbers and append
for i in range(10):
    num = int(input(f"Enter number {i+1}: "))
    numbers.append(num)

# initialize 'seen' list
seen = []

# check if numbers are in seen list
for num in numbers:
    if num not in seen:
        
# print number and append to seen list
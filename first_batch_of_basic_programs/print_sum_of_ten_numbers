# initialize zero 
total_sum = 0

# prompt for ten numbers via loop 
for i in range(10):
    number = float(input(f"Enter number {i+1}: "))
    
# add and print sum
    total_sum += number
print(f"The sum is: {total_sum}.")
